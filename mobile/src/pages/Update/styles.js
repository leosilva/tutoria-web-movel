import {StyleSheet} from "react-native";

export default StyleSheet.create({
    productView: {
        maxWidth: 700,
        padding: 20
    },

    productViewTitle: {
        fontSize: 24,
        fontWeight: 'bold'
    }
});